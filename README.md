# SheetC

SheetC is a terminal-based spreadsheet engine written in C.  
It allows users to assign values and formulas to cells, supports range functions, automatically recalculates dependent cells, and efficiently handles large grids using scrolling.
